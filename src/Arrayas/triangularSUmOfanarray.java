package Arrayas;

public class triangularSUmOfanarray {
/*
    public static void main(String[] args) {
        int[] ques ={1,2,3,4,5};
        triangularSum(ques);
    }


    public static String addBinary(String a, String b) {
        int len1 = a.length()-1;
        int len2 = b.length()-1;
        String c = new String();
        while(len1>0&&len2>0){


            c.charAt(len1) = (char) (a.charAt(len1)+b.charAt(len2));
            if(c.charAt(len1)>1) c.charAt(len1)=0; c.charAt(len1+1)=1;

            len1--;
            len2--;
        }

        return c;*/
    }
   /* public static int triangularSum(int[] nums) {
        int n=nums.length;
        while(n != 1){

            for(int i=0;i<n-1;i++)
                nums[i] = (nums[i] + nums[i+1]) % 10;
            n--;
        }

        return nums[0];
    }*/

